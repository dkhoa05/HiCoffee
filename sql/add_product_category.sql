-- =============================================================================
-- FILE NÀY ĐÃ LỖI THỜI — mọi thứ đã gộp vào:  sql/reset_fresh.sql
-- Chỉ chạy reset_fresh.sql để tạo lại DB đầy đủ (gồm cột category).
-- =============================================================================
